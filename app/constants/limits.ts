/**
 * Ліміти з PRD §14.2 і §9.4.
 *
 * Значення зібрані в одному файлі, бо між ними є зв'язки, які легко зламати
 * правкою в одному місці — див. коментар до MAX_PARTICIPANTS.
 */

/**
 * PRD §14.2, §18 (п. 2 і 13).
 *
 * ⚠️ Це число ЖОРСТКО пов'язане з розміром набору аватарів: аватари унікальні
 * в межах кімнати, тому набір з N зображень означає кімнату максимум на N осіб.
 * Два числа переглядаються тільки разом. Інваріант перевіряється тестом
 * у test/avatars.spec.ts — саме тому, що при правці «на око» він мовчки падає.
 */
export const MAX_PARTICIPANTS = 30

/** PRD §9.4 — довше ламає верстку лідерборду на телевізорі. */
export const PARTICIPANT_NAME_MAX_LENGTH = 20

/** PRD §9.3 — шестизначний числовий код, який диктується вголос. */
export const ROOM_CODE_LENGTH = 6

/** PRD §14.2 — зображення до 5 МБ, стиснення автоматичне. */
export const IMAGE_MAX_BYTES = 5 * 1024 * 1024

/**
 * PRD §14.1 — таймер оголошується скрінрідером ТІЛЬКИ на цих порогах.
 * Оголошення щосекунди робить гру непридатною для користувача скрінрідера.
 */
export const TIMER_ANNOUNCE_THRESHOLDS_SEC = [60, 30, 10, 0] as const

/** PRD §14.2 — цільова затримка синхронізації трьох поверхонь. */
export const SYNC_LATENCY_BUDGET_MS = 300
